import { Alphabetvalidatordirective } from './alphabetvalidatordirective';

describe('Alphabetvalidatordirective', () => {
  it('should create an instance', () => {
    expect(new Alphabetvalidatordirective()).toBeTruthy();
  });
});
